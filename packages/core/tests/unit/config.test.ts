// Tests for config module are in tests/unit/config/config.test.ts
// This file exists as an empty module to satisfy older test runners.
import { it, expect } from 'vitest';
it('config module re-export is valid', () => { expect(true).toBe(true); });
